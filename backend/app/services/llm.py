class LLMService:
    """Placeholder integration: replace with OpenAI/Azure/HF client."""

    def generate(self, prompt: str) -> str:
        return (
            "Based on your history, I recommend hydration, sleep hygiene, "
            "and scheduling a physician review if symptoms persist."
        )
